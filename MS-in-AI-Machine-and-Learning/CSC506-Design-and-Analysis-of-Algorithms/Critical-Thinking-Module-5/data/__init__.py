"""Dataset generation, validation, and preview utilities for hash table and heap scenarios."""

from data.dataset_manager import (
    generate_key_value_dataset,
    generate_priority_items,
    generate_search_queries,
    generate_collision_keys,
    preview_records,
    validate_key_value_dataset,
    validate_priority_items,
    DEFAULT_SEED,
)
